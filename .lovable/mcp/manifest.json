{
  "version": 1,
  "sdk_version": "0.26.1",
  "path": "/functions/v1/mcp",
  "auth": {
    "type": "oauth",
    "issuer": "https://qhtntebpcribjiwrdtdd.supabase.co/auth/v1",
    "accepted_audiences": [
      "authenticated"
    ]
  },
  "mcp": {
    "server": {
      "name": "apex-psi",
      "version": "0.2.0",
      "title": "APEX PSI"
    },
    "tools": [
      {
        "name": "seal",
        "title": "Seal a decision to the APEX PSI ledger",
        "description": "Mint a post-quantum verifiable receipt for a machine action or decision. Canonicalises the payload, signs it (SHA-256 + Ed25519 + LMS-W4-SHA256), records it in the append-only ledger, and returns a receipt id that any agent can later re-check with verify_hash. This is the write half of the trust handshake.",
        "annotations": {
          "readOnlyHint": false,
          "idempotentHint": false,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "decision": {
              "type": "string",
              "minLength": 1,
              "maxLength": 10000,
              "description": "The action or decision text to seal — what the machine did."
            },
            "model_id": {
              "description": "Optional identifier of the model or agent that produced the decision.",
              "type": "string",
              "maxLength": 200
            },
            "context": {
              "description": "Optional description of the decision environment.",
              "type": "string",
              "maxLength": 2000
            },
            "predicate": {
              "description": "Optional predicate id, e.g. EU_ART_50. Defaults to EU_ART_12.",
              "type": "string",
              "maxLength": 100
            }
          },
          "required": [
            "decision"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "verify_hash",
        "title": "Verify a hash against the APEX PSI ledger",
        "description": "Look up a SHA-256 hash (commit hash or Merkle leaf hash) in the APEX PSI evidence ledger and report whether a matching attestation exists.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "hash": {
              "type": "string",
              "pattern": "^[0-9a-fA-F]{64}$",
              "description": "SHA-256 hash to verify, lowercase hex."
            }
          },
          "required": [
            "hash"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "anchor_status",
        "title": "Bitcoin anchoring status of a sealed receipt",
        "description": "Report the OpenTimestamps / Bitcoin anchoring state for a sealed receipt: whether its Merkle root has been committed to a real Bitcoin block, the block height, the transaction id and the offline `ots verify` command. Anchoring runs automatically; this reads its result — it does not trigger it.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "commit_id": {
              "type": "string",
              "description": "Receipt id returned by `seal`, e.g. APEX-NTR-...."
            }
          },
          "required": [
            "commit_id"
          ]
        },
        "outputSchema": null
      },
      {
        "name": "list_attestations",
        "title": "List recent attestations",
        "description": "List the most recent APEX PSI ledger attestations visible to the signed-in user, newest first.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "limit": {
              "default": 10,
              "description": "How many attestations to return.",
              "type": "integer",
              "minimum": 1,
              "maximum": 50
            },
            "predicate_id": {
              "description": "Optional predicate filter, e.g. EU_ART_50.",
              "type": "string"
            }
          }
        },
        "outputSchema": null
      },
      {
        "name": "ledger_stats",
        "title": "APEX PSI ledger statistics",
        "description": "Return counts of ledger attestations, non-approved exceptions and public attestations for a quick integrity snapshot.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {}
        },
        "outputSchema": null
      },
      {
        "name": "protocol_info",
        "title": "APEX PSI protocol reference",
        "description": "Return the APEX PSI protocol reference: canonicalization, hashing, signature suites, IETF drafts, HTTP header and anchoring targets.",
        "annotations": {
          "readOnlyHint": true,
          "idempotentHint": true,
          "openWorldHint": false
        },
        "inputSchema": {
          "$schema": "http://json-schema.org/draft-07/schema#",
          "type": "object",
          "properties": {
            "section": {
              "default": "all",
              "description": "Which part of the reference to return.",
              "type": "string",
              "enum": [
                "all",
                "signatures",
                "drafts",
                "anchoring"
              ]
            }
          }
        },
        "outputSchema": null
      }
    ]
  }
}
